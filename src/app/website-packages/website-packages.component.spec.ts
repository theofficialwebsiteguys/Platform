import { ComponentFixture, TestBed } from '@angular/core/testing';

import { WebsitePackagesComponent } from './website-packages.component';

describe('WebsitePackagesComponent', () => {
  let component: WebsitePackagesComponent;
  let fixture: ComponentFixture<WebsitePackagesComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [WebsitePackagesComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(WebsitePackagesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
